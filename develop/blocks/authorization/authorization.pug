mixin authorization()
    +b.SECTION.authorization
        .wrapper
            .logo
                +icon('logo-full', 'fill')
            .content
                form.form(action='#' method='post')
                    h1.form-title Авторизация
                    .input-row
                        input(type='email' name='authEmail' id='auth-email')
                        label(for='auth-email') E-mail
                    .input-row
                        input(type='password' name='authPassword' id='auth-password')
                        label(for='auth-email') Пароль
                    .actions
                        button.submit(type='submit') Войти
                        .login-socials
                            a.login-social(href='#')
                                +icon('vk', 'fill')
                            a.login-social(href='#')
                                +icon('fb', 'fill')
                            a.login-social(href='#')
                                +icon('twitter', 'fill')
                    a.legal-link(href='#') Политика конфиденциальности
                    a.legal-link(href='#') Как получить код авторизации?
                .info
                    h2.info-title Образовательная онлайн-платформа&nbsp;SkillBranch
                    p.info-text Это студии профессиональной видеозаписи, команда методистов, тех.поддержка, индивидуальная проверка домашних заданий в онлайне, а также большое сообщество наших студентов и выпускников