mixin icon(name, blockName, mod)
    -var mod = mod || ''
    -var svgClass = blockName ? blockName + '__svg ' + mod : ''
    -var useClass = blockName ? blockName + '__use ' + mod : ''
        svg(class=svgClass)
            use( class=useClass xlink:href="./images/html-sprite.svg#" + name)